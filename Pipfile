[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
wallet-watch = {file = ".", editable = true}
pytelegrambotapi = ">=4.14.0"
requests = ">=2.31.0"
python-dotenv = ">=1.0.0"
pyyaml = ">=6.0"
base58 = ">=2.1.0"
pydantic = ">=2.0.0"
click = ">=8.1.0"
flask = ">=3.0.0"

[dev-packages]
pytest = ">=7.4.0"
pytest-cov = ">=4.1.0"
ruff = ">=0.1.0"

[requires]
python_version = "3.10"
